import iconConfig from "@/config/icons/icon-config";

export const MapProviderIcon: Record<string, React.ReactNode> = {
  credentials: iconConfig.user2.base,
  google: iconConfig.googleColor.base,
  facebook: iconConfig.facebookColor.base,
};
